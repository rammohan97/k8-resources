apiVersion: v1
kind: Pod
metadata:
  name: error-test
spec:
  containers:
    # - name: nginx
    #   image: nginxddwd
    #   ports:
    #     - containerPort: 80
    - name: almalinux
      image: almalinux:9